<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AttendanceApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app</item>
        <item name="colorPrimaryVariant">@color/app</item>
        <item name="colorOnPrimary">@color/app</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/app</item>
        <item name="colorSecondaryVariant">@color/app</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/app</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="PopupMenuMoreCenteralized" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:dropDownHorizontalOffset">4dp</item>
        <item name="android:dropDownVerticalOffset">-15dp</item>

    </style>

</resources>